<configuration>

    <timestamp key="startupDate" datePattern="dd-MM-yyyy" timeReference="contextBirth" />

    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss dd/MM/yy} | %-5level | %logger{36} | %msg%n</pattern>
        </encoder>
    </appender>

    <appender name="FILE" class="ch.qos.logback.core.FileAppender">
        <file>logs/smart-device-manager-${startupDate}.log</file>
        <append>true</append>
        <encoder>
            <pattern>%d{HH:mm:ss dd/MM/yy} | %-5level | %logger{36} | %msg%n</pattern>
        </encoder>
    </appender>

    <root level="INFO">
        <appender-ref ref="CONSOLE" />
        <appender-ref ref="FILE" />
    </root>

</configuration>
